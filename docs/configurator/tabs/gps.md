# GPS

The GPS tab configures GPS module settings and shows live GPS status
(fix type, satellite count, position). If GPS is fitted, this is also where
[GPS Rescue](../../flight-modes/gps-rescue.md) behavior is configured.
