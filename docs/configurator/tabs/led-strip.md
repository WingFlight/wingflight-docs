# LED Strip

The LED Strip tab configures addressable RGB LED strips connected to the
flight controller -- layout, colors, and functions (e.g. showing armed
state, flight mode, or orientation).

LED types, wiring, the available functions and colors, and the CLI syntax are
described in the [LED Strip reference](../../reference/led-strip.md).
