# Ready-to-Arm Wiggle

Ready-to-Arm Wiggle gives a visual confirmation that the flight controller
has armed by wiggling the control surfaces, useful when you can't hear a
beeper (e.g. a noisy field) or don't have LEDs in view. It's a small
usability feature, but a genuinely useful one for confirming arm state at a
glance.
